/**
 * API Mocks for Build Compatibility
 * Simplified version for production build
 */

// Simplified mock handlers for build compatibility
export const apiMockHandlers: any[] = [];

// Export default for compatibility
export default apiMockHandlers;